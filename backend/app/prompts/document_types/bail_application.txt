{% include "base_rules.txt" %}

{% if opening_paras %}
MANDATORY PARAGRAPHS (Opening):
{% for para in opening_paras %}
- {{ para.instruction }}
{% endfor %}
{% endif %}

{% if structure_rules %}
STRUCTURE RULES (apply strictly):
{% for rule in structure_rules %}
- {{ rule.rule_description }}
  {% if rule.is_heading %}(write as a labeled heading{% if rule.overridden_by_user %}, per explicit user override — this is an intentional exception to standard practice{% endif %}){% else %}(write as a plain numbered paragraph, NO heading){% endif %}
{% endfor %}
{% endif %}

{% if body_paras %}
MANDATORY PARAGRAPHS (Body) — EVERY item below MUST appear as its own numbered paragraph in the main petition. Do NOT skip, merge, condense, or omit any item:
{% for para in body_paras %}
- {{ para.instruction }}
{% endfor %}
{% endif %}


DOCUMENT TYPE: {{ doc_type }} for {{ court_display }}

{% if court_rules_block %}
<<FORMATTING_INSTRUCTIONS_ONLY — DO NOT REPRODUCE IN DOCUMENT — FOR YOUR SILENT REFERENCE ONLY>>
{{ court_rules_block }}
<<END_FORMATTING_INSTRUCTIONS>>

{% endif %}
CASE DETAILS (Bail Application):
- Applicant/Accused: {{ petitioners }}
- State / Complainant: {{ respondents }}
- Advocate: {{ form_data.advocate_name | default('Not provided') }} (Enrollment: {{ form_data.advocate_enrollment_no | default('') }})
- Crime Number / FIR No.: {{ form_data.jurisdiction_basis | default('Not provided — fill from uploaded FIR') }}
- Date of Arrest: {{ form_data.impugned_order_date | default('Not provided') }}

FACTS:
{{ form_data.facts_of_case }}

DATES AND EVENTS (reproduce ALL rows VERBATIM in the LIST_OF_DATES section as a markdown table — do NOT alter, reorder, or omit any row):
{% if form_data.dates_and_events %}
| Date | Event |
|------|-------|
{% for de in form_data.dates_and_events %}| {{ de.date }} | {{ de.event }} |
{% endfor %}{% else %}No dates provided — use ___/___/{{ current_year }} for filing date only.{% endif %}

GROUNDS FOR BAIL (user-provided):
{{ form_data.grounds | default('Not provided — draft standard bail grounds based on facts') }}

RELIEF SOUGHT (reproduce VERBATIM and IN FULL in the Prayer clause — do NOT omit, paraphrase, or truncate any detail including qualifying phrases):
{{ form_data.relief_sought }}

{{ uploaded_docs_context }}

RELEVANT STATUTES:
{{ statutes_block }}

RELEVANT PRECEDENTS:
{{ judgments_block }}

SPECIFIC DRAFTING INSTRUCTIONS (Bail Application):
1. FIRST PAGE must prominently show (per court rules if applicable): Crime Number, Police Station, Section(s) and Act/Rules under which applicant is prosecuted/convicted, and whether this is the first, second, or subsequent application before this court.
2. Draft a paragraph disclosing: whether any earlier bail application has been made before the Magistrate and/or Sessions Judge, and the result of such application(s). This is MANDATORY.
3. The application must be accompanied by: copy of the FIR (if uploaded), date of alleged occurrence, date of applicant's arrest.
4. Include grounds: (a) nature of offence and evidence, (b) applicant's roots in society / no flight risk, (c) no likelihood of tampering with evidence, (d) applicant's health/age/occupation if relevant, (e) parity with co-accused if applicable.
5. State the Government Advocate notice requirement if at High Court level (not less than 10 days unless exempted for urgency).
6. Number every paragraph consecutively.

REQUIRED SECTIONS — write these in this exact order, each wrapped with <<SECTION:NAME>>...content...<<END_SECTION>>:
1. COVER_AND_INDEX
2. MAIN_APPLICATION
3. PRAYER
4. AFFIDAVIT
5. VAKALATNAMA
6. ANNEXURES

⚠️ FINAL REMINDER BEFORE DRAFTING: Every paragraph in MAIN_APPLICATION MUST start with its sequential number (1. 2. 3. …). No paragraph may appear without a number. Grounds use A. B. C. — not numbers. Begin drafting now.
