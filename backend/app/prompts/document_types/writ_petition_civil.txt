{% include "base_rules.txt" %}

{% if opening_paras %}
MANDATORY PARAGRAPHS (Opening):
{% for para in opening_paras %}
- {{ para.instruction }}
{% endfor %}
{% endif %}

{% if structure_rules %}
STRUCTURE RULES (apply strictly):
{% for rule in structure_rules %}
- {{ rule.rule_description }}
  {% if rule.is_heading %}(write as a labeled heading{% if rule.overridden_by_user %}, per explicit user override — this is an intentional exception to standard practice{% endif %}){% else %}(write as a plain numbered paragraph, NO heading){% endif %}
{% endfor %}
{% endif %}

{% if body_paras %}
MANDATORY PARAGRAPHS (Body) — EVERY item below MUST appear as its own numbered paragraph in the main petition. Do NOT skip, merge, condense, or omit any item:
{% for para in body_paras %}
- {{ para.instruction }}
{% endfor %}
{% endif %}


DOCUMENT TYPE: {{ doc_type }} for {{ court_display }}

{{ jurisdiction_warning }}

{% if court_rules_block %}
<<FORMATTING_INSTRUCTIONS_ONLY — DO NOT REPRODUCE IN DOCUMENT — FOR YOUR SILENT REFERENCE ONLY>>
{{ court_rules_block }}
<<END_FORMATTING_INSTRUCTIONS>>

{% endif %}
CASE DETAILS:
- Petitioner(s): {{ petitioners }}
- Respondent(s): {{ respondents }}
- Advocate: {{ form_data.advocate_name | default('Not provided') }} (Enrollment: {{ form_data.advocate_enrollment_no | default('') }})
- Jurisdiction Basis: {{ form_data.jurisdiction_basis | default('') }}
- Impugned Order Date: {{ form_data.impugned_order_date | default('Not applicable') }}

FACTS:
{{ form_data.facts_of_case }}

DATES AND EVENTS (reproduce ALL rows VERBATIM in the LIST_OF_DATES section as a markdown table — do NOT alter, reorder, or omit any row):
{% if form_data.dates_and_events %}
| Date | Event |
|------|-------|
{% for de in form_data.dates_and_events %}| {{ de.date }} | {{ de.event }} |
{% endfor %}{% else %}No dates provided — use ___/___/{{ current_year }} for filing date only.{% endif %}

GROUNDS (user-provided):
{{ form_data.grounds | default('Not provided — draft standard grounds based on facts') }}

RELIEF SOUGHT (reproduce VERBATIM and IN FULL in the Prayer clause — do NOT omit, paraphrase, or truncate any detail including qualifying phrases):
{{ form_data.relief_sought }}

INTERIM RELIEF:
{{ form_data.interim_relief_sought | default('Not specifically requested') }}

{{ uploaded_docs_context }}

RELEVANT STATUTES:
{{ statutes_block }}

RELEVANT PRECEDENTS:
{{ judgments_block }}

REQUIRED SECTIONS — write these in this exact order, each wrapped with <<SECTION:NAME>>...content...<<END_SECTION>>:
1. COVER_AND_INDEX
2. SYNOPSIS
3. LIST_OF_DATES
4. MAIN_PETITION
5. PRAYER
6. AFFIDAVIT
7. VAKALATNAMA
8. ANNEXURES

⚠️ FINAL REMINDER BEFORE DRAFTING: Every paragraph in SYNOPSIS and MAIN_PETITION MUST start with its sequential number (1. 2. 3. …). No paragraph may appear without a number. Grounds use A. B. C. — not numbers. Begin drafting now.
