# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database  (asyncpg is the async driver; psycopg2-binary is NOT needed
#            and was adding ~20MB of dead weight)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
alembic==1.14.0

# AWS / Storage
boto3==1.35.76

# ML / Search
# sentence-transformers pulls torch as a dep; torch itself is installed
# separately in the Dockerfile from the CPU-only wheel index.
sentence-transformers==3.3.1

# Utils
python-dotenv==1.0.1
