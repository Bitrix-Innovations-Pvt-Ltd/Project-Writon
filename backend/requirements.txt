# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart

# PyTorch CPU-only (CUDA variant is ~6GB; CVE-2025-32434 requires >=2.6)
# Installed via --index-url https://download.pytorch.org/whl/cpu in Dockerfile
torch==2.6.0

# Database  (asyncpg is the async driver; psycopg2-binary is NOT needed
#            and was adding ~20MB of dead weight)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
alembic==1.14.0

# AWS / Storage
boto3==1.35.76

# ML / Search
sentence-transformers==3.3.1

# Utils
python-dotenv==1.0.1

# OCR & File Processing
pytesseract
Pillow
PyMuPDF

# LLM Integrations
openai
